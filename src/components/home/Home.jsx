import React from 'react';
import Navbar from '../navbar/Navbar';
import { Outlet } from 'react-router';

const Home = () => {
    return (
        <div>
            home
        </div>
    );
};

export default Home;